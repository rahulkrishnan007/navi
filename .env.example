# --- Database ---------------------------------------------------------------
# SQLite (default, zero-config local dev):
DATABASE_URL="file:./dev.db"
# Postgres (production): postgresql://user:password@host:5432/career_navigator

# --- Auth --------------------------------------------------------------------
# Long, random secret used to sign JWTs. Generate one with:
#   openssl rand -base64 48
JWT_SECRET="replace-this-with-a-long-random-string"

# --- AI (optional but required for the AI Summary feature) -------------------
# Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=""
